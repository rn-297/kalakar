import 'package:flutter/material.dart';

class ArtistDocumentsFormPage extends StatelessWidget {
  const ArtistDocumentsFormPage({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      body: Center(
        child: Text(
          "Artist Document Form",
        ),
      ),
    );
  }
}
