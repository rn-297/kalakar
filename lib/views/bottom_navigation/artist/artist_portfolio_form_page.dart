import 'package:flutter/material.dart';

class ArtistPortfolioFormPage extends StatelessWidget {
  const ArtistPortfolioFormPage({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      body: Center(
        child: Text(
          "Artist portfolio Form",
        ),
      ),
    );
  }
}
