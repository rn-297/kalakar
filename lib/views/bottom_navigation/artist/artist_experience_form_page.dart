import 'package:flutter/material.dart';

class ArtistExperienceFormPage extends StatelessWidget {
  const ArtistExperienceFormPage({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      body: Center(
        child: Text(
          "Artist Experience Form",
        ),
      ),
    );
  }
}
