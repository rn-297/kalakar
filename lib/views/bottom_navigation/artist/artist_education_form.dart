import 'package:flutter/material.dart';

class ArtistEducationFormPage extends StatelessWidget {
  const ArtistEducationFormPage({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      body: Center(
        child: Text(
          "Artist Education Form",
        ),
      ),
    );
  }
}
